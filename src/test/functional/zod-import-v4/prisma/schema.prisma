// Test for zodImportPath = "zod/v4"

datasource db {
  provider = "postgresql"
  url      = ""
}

generator client {
  provider = "prisma-client-js"
  output   = ".client"
}

generator zod {
  provider      = "zod-prisma"
  output        = "../actual/"
  zodImportPath = "zod/v4"
}

model User {
  id    String @id @default(cuid())
  email String @unique
  name  String
}

enum Role {
  USER
  ADMIN
}
